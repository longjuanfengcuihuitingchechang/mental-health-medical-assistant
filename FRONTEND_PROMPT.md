# 心理健康专项智能医疗助手前端改造提示词

你是一名资深医疗 Web 前端工程师。请直接修改当前项目的 `fronts/` 目录，把现有通用医疗系统静态页面改造成“心理健康专项智能医疗助手”前端。开始编码前先完整阅读：

- `TASK_STRUCTURE.md`
- `SPECS/产品需求文档.md`
- `SPECS/ARCHITECTURE.md`
- `SPECS/技术规格说明.md`
- `SPECS/接口文档.md`

如果本文与上述规格冲突，以 `SPECS/接口文档.md` 的 API 字段和 `SPECS/产品需求文档.md` 的安全边界为准。只改 `fronts/`，不要修改后端、规格文档、`.env`、CI/CD 或 E 盘数据库。

## 一、现状与目标

`fronts/` 当前包含 15 个 Tailwind CDN 静态 HTML 页面：登录、注册，管理员 5 页，医生 6 页，患者 3 页。页面主要是硬编码演示数据；登录和注册仅用内联 JavaScript 模拟跳转；没有统一 API 客户端、身份状态、加载/错误状态或 SSE。

改造目标：

1. 保留现有 Tailwind 视觉基础、三角色布局和相对路径导航。
2. 将产品定位从综合医疗管理调整为心理健康筛查、情绪支持、风险复核和随访助手。
3. 接入 `/api/v1` 目标接口；后端未就绪时支持显式 mock 模式，禁止把 mock 成功伪装成真实成功。
4. 所有页面具备加载、空数据、错误、断网、无权限和 E 盘数据库不可用状态。
5. 高风险流程不依赖大模型回复；必须优先显示现实世界求助卡。

## 二、技术约束

- 继续采用 HTML + Tailwind CSS + 原生 JavaScript，不引入 React/Vue 或新的包管理器。
- 提取共享脚本和样式，避免每个页面重复内联业务逻辑。
- 建议新增：

```text
fronts/
├── assets/
│   ├── css/app.css
│   └── js/
│       ├── config.js
│       ├── api.js
│       ├── auth.js
│       ├── ui.js
│       ├── sse.js
│       └── mock-data.js
├── admin/
├── doctor/
├── patient/
└── ...
```

- `api.js` 统一实现 `fetch`、Cookie 会话、JSON 解析、`X-Request-ID`、错误转换、超时和 `Idempotency-Key`。
- 请求使用 `credentials: "include"`；不得把密码、会话 Cookie、长期 token 或敏感对话保存到 `localStorage`。
- “记住我”只能保存非敏感偏好，例如上次选择的角色；不能保存账号密码或会话 token。
- API Base URL 默认为 `/api/v1`，集中配置，页面不得散落硬编码地址。
- mock 模式必须在页面顶部显示“演示数据，未写入医疗记录”标识；生产模式默认关闭。
- 当前 Tailwind CDN 可暂时保留用于原型，但必须集中标记为待构建替换项，禁止新增第三方 CDN。

## 三、统一视觉与交互规范

### 1. 品牌与色彩

- 产品名统一为“心晴助手”或“心理健康智能医疗助手”，副标题为“筛查、支持与专业协同”。
- 患者端主色使用温和的青绿/蓝绿色，避免过度医疗化和刺眼警示。
- 医生端保留深灰侧栏和 teal 主色。
- 管理端保留蓝色体系。
- 风险颜色只表达工作流优先级：低风险绿色、中风险琥珀、高风险橙红、紧急风险红色；不要用颜色作为唯一信息载体。

### 2. 公共组件

所有页面统一实现：

- 桌面侧栏、移动端抽屉导航、顶部栏、当前用户菜单和退出。
- `LoadingSkeleton`、`EmptyState`、`ErrorState`、`PermissionState`、`OfflineBanner`。
- Toast 只用于轻量反馈；表单错误必须显示在对应字段旁。
- 模态框具备焦点锁定、Esc 关闭、关闭后焦点归还和明确标题。
- 所有按钮有 disabled/loading 状态，提交期间防止重复请求。
- 时间按用户时区展示，并在详情中允许查看完整时间。
- 列表采用游标分页，不假定总页数；`limit` 不超过 100。

### 3. 可访问性与响应式

- 支持键盘完整操作、可见焦点、语义化标题、label、aria-live 和屏幕阅读器文本。
- 图标按钮必须有可理解的 `aria-label`，图表必须有文本摘要。
- 360px 宽度下不能横向溢出；表格在移动端转为卡片或可控横向滚动。
- 危机支持卡不能被普通 toast 替代，不能自动消失。

## 四、页面改造

### 1. `index.html`：登录

- 标题改为“心理健康智能医疗助手”。
- 保留管理员、医生、患者角色选择。
- 对接 `POST /api/v1/auth/login`，成功后调用 `GET /api/v1/auth/session` 验证角色，再进入对应首页。
- 401 显示统一错误，不提示账号是否存在；429 显示稍后重试。
- 数据库不可用 `423 DATABASE_UNAVAILABLE` 时显示“数据存储设备不可用，请联系管理员”，不得继续模拟登录。
- 删除当前按角色直接跳转的假登录逻辑。

### 2. `register.html`：注册与隐私告知

- 注册表单保留姓名、联系方式、角色、密码和确认密码。
- 分离“用户协议”“隐私政策”“AI 助手单独同意”，不得用一个复选框代替全部同意。
- AI 同意不是普通账号注册的强制条件；未同意时仍可使用非 AI 功能。
- 提交目标接口暂标记为 `/api/v1/registration-requests`，在接口未实现时显示“该功能尚未开放”，不能弹出虚假注册成功。

### 3. 患者端导航与页面

患者端导航统一为：

```text
心理健康概览
智能支持
心理测评
情绪日记
随访与资源
个人与隐私
```

#### `patient/index.html`

- 改为心理健康概览：欢迎区、今日情绪打卡、最近一次筛查、7/30 天趋势、待办随访、快捷求助资源。
- 移除血常规、处方、普通医疗报告等非专项核心硬编码。
- 数据来自 `/me`、`/wellbeing-trends`、本人量表结果、`/followups`、`/support-resources`。
- 量表分数只展示服务端结果；注明“筛查结果不等于诊断”。

#### `patient/online-consultation.html`

- 改为“智能支持”主工作台，不冒充真人医生。
- 三栏/两栏结构：会话列表、消息区、可折叠支持资源区。
- 首次进入显示非诊断声明、隐私提示和 AI 同意状态。
- 对接 `/conversations`、消息历史和 `/messages/stream` SSE。
- 展示 `message.accepted`、`safety.result`、`message.delta`、`message.completed`、`crisis.support_required` 和 `error`。
- SSE 断开时保留已收到内容，显示重连按钮；不能把中断消息标记为完成。
- 命中 `crisis.support_required` 时立即在消息区顶部固定显示危机支持卡，内容包括：
  - “如果你或他人正处于立即危险中，请优先联系现实世界的紧急帮助。”
  - `12356` 全国统一心理援助热线。
  - 由 `/support-resources` 返回的本地资源。
  - `110/120` 等紧急渠道及“联系身边可信任的人”。
  - 明确显示通知状态；没有服务端回执时不得写“已通知医生/急救”。
- 输入区禁止“上传用药清单”等非专项文案，改为发送文本、打开量表、查看资源。

#### 将 `patient/vital-signs-checkin.html` 改为“情绪日记”

- 表单字段：日期、情绪 1–5、压力 1–5、睡眠时长、睡眠质量 1–5、可选备注。
- 提交 `POST /wellbeing-entries`；编辑使用 `PATCH /wellbeing-entries/{id}`。
- 展示最近 7/30 天趋势和文本摘要；图表没有数据时显示空状态。
- 前端不根据单个数字生成“抑郁/焦虑诊断”。

#### 新增 `patient/assessments.html`

- 已发布量表卡片、预计耗时、适用说明、历史记录和“继续未完成测评”。
- 从 `/assessments` 获取量表，不把题目和阈值硬编码在页面。
- 创建 `/assessment-sessions`，保存答案到 `/answers`，提交 `/submit`。
- 支持进度、上一题/下一题、未答检查、提交确认、服务端校验错误定位。
- 结果页展示服务端得分、解释、风险摘要、规则版本和完成时间；必须显示非诊断声明。

#### 新增 `patient/resources.html`

- 展示随访任务、专业帮助资源、隐私与 AI 同意入口。
- 支持查看和撤回 `/me/consents`；撤回前解释影响。
- 资源标明地区、服务时间、最后核验日期；未核验的 24 小时属性不得擅自显示。

### 4. 医生端导航与页面

医生端导航统一为：

```text
工作台
我的来访者/患者
风险事件
在线支持
筛查与趋势
随访管理
个人中心
```

#### `doctor/index.html`

- 统计卡改为：高风险待确认、中风险待随访、今日随访、未读对话。
- 主列表展示高优先级风险事件和今日随访，不展示待审核处方。
- 风险卡显示等级、触发时间、证据类型摘要、当前负责人、处置状态。

#### `doctor/my-patients.html`

- 显示负责患者、最近筛查、趋势、最近联系和随访状态。
- 操作改为“查看心理档案”“发起随访”“发送量表”“进入对话”。
- 移除“开具处方”快捷按钮；AI 不提供处方入口。
- 数据范围由后端强制，前端遇到 403 显示无权限状态。

#### `doctor/online-consultation.html`

- 保留会话工作台布局，增加患者授权状态、风险徽标、筛查摘要和危机资源入口。
- 医生消息与 AI 建议必须有清晰身份区别。
- 查看完整对话正文前，如接口要求访问理由，弹出理由表单并形成审计。
- “发送量表”从 `/assessments` 获取已发布版本，不硬编码。

#### 将 `doctor/prescriptions-records.html` 改为 `doctor/risk-events.html`

- 页面标题改为“风险事件处置”。
- 左侧/顶部筛选：风险等级、状态、负责人、日期、患者关键词。
- 详情包含证据摘要、量表/文本来源、规则版本、处置时间线。
- 使用 `/risk-events` 和 `/risk-events/{id}/actions`。
- 支持 `ACKNOWLEDGED`、`CONTACT_ATTEMPTED`、`CONTACT_CONFIRMED`、`REFERRED`、`FALSE_POSITIVE`、`CLOSED`。
- 关闭必须填写理由并进行二次确认；采用乐观并发版本，409 时重新加载。
- 不允许编辑或删除既有处置历史。

#### 将 `doctor/schedule-appointments.html` 调整为“随访管理”

- 保留日历/列表视觉，内容改为待随访、已安排、已完成、已逾期。
- 对接 `/followups`、`PATCH /followups/{id}`、`POST /followups/{id}/notes`。
- 创建随访只代表内部任务；没有外部预约确认接口时不得显示“患者已确认”。

#### `doctor/profile.html`

- 保留个人资料，新增专业资质展示占位和通知偏好。
- 敏感通知渠道标注由管理员配置；前端不保存密钥。

### 5. 管理员端导航与页面

管理员导航统一为：

```text
系统概览
患者管理
专业人员管理
注册申请
量表与资源
安全审计
数据与备份
```

#### `admin/index.html`

- 展示账号数、活跃风险事件、未处置高风险、数据库状态和最后验证备份时间。
- 数据库异常用明确维护卡展示；不要暴露密钥、数据库连接密码或完整内部堆栈。

#### `admin/patients.html` 与 `admin/doctors.html`

- 对接 `/admin/users` 和 `/admin/care-assignments`。
- 用户停用替代直接删除。
- 患者列表默认不展示完整证件号、手机号、详细心理内容。

#### `admin/registration-requests.html`

- 保留审批页面，但接口未定稿时置为功能开关关闭状态。
- 批量审批必须二次确认并逐条显示结果，禁止把部分失败显示成全部成功。

#### 将 `admin/data-import.html` 改为“数据与备份”

- 主要展示 `/admin/system/database-status` 和 `/admin/system/backups`。
- 显示 E 盘要求、数据库健康、剩余空间、WAL、完整性检查和最后验证备份。
- 创建备份前二次确认；仅展示任务状态，不直接让浏览器访问数据库文件路径。
- 数据导入保留为次级区域并标明“受控导入”，后端接口未实现时禁用。

#### 新增 `admin/assessment-resources.html`

- 管理量表草稿/发布版本和支持资源。
- 已发布量表版本只读，修改必须新建版本。
- 发布操作显示版本、题目数、计分规则版本、审核人和二次确认。
- 资源管理包含地区、服务时间、来源、最后核验日期、启用状态。

#### 新增 `admin/audit.html`

- 按操作者、对象、动作和时间查询审计事件。
- 默认不展示完整对话正文；敏感访问需额外权限。

## 五、接口和状态处理

严格使用 `SPECS/接口文档.md` 的响应结构。统一处理：

| 状态 | 前端行为 |
|---|---|
| 400/422 | 映射到表单字段或答题项 |
| 401 | 清理内存会话状态并回登录页，保留安全的返回路径 |
| 403 | 展示无权限页面，不把资源当作不存在之外的信息泄漏给用户 |
| 404 | 展示资源不存在或不可见 |
| 409 | 提示数据已更新，重新拉取并保留用户未提交文本 |
| 423 | 显示 E 盘数据库不可用维护状态，禁用写按钮 |
| 429 | 显示重试提示和可用的等待时间 |
| 503 | 智能对话降级；量表和危机资源仍保持可访问 |

所有写按钮都支持幂等键；页面刷新后不得自动重复提交量表、风险处置或随访记录。

## 六、禁止事项

- 不在前端计算可信量表总分、标准分、解释或风险等级。
- 不展示“你患有抑郁症/焦虑症”等诊断性结论。
- 不让大模型关闭、降低或删除风险事件。
- 不在没有回执时显示“已通知医生”“已联系急救”。
- 不保存密码、token、完整对话或心理量表答案到 localStorage。
- 不使用真实姓名、证件号、手机号或真实心理记录作为静态演示数据。
- 不新增处方、药物剂量建议、自动诊断或强制干预功能。
- 不直接连接、读取或下载 E 盘 SQLite 数据库。

## 七、验收标准

完成后逐项自检并提交变更摘要：

1. 15 个现有页面均完成保留、改造或明确替代，新页面导航完整且无死链。
2. 三角色登录根据真实会话跳转，不再按下拉框直接假登录。
3. 患者可以完成情绪日记、量表流程和智能支持演示；所有结果来自 API/mock 契约。
4. 高风险 SSE 事件必定显示固定危机支持卡，断网和模型故障不隐藏现实求助资源。
5. 医生可以查看风险队列并追加不可篡改的处置记录，关闭时必须填写理由。
6. 管理员可以查看 E 盘数据库健康和备份状态，但看不到密钥或无关敏感正文。
7. 所有页面覆盖 loading、empty、error、offline、403、423 和 503 状态。
8. 360px、768px、1440px 三种宽度无关键操作遮挡或不可访问。
9. 键盘导航、焦点、表单 label、aria-live 和颜色之外的风险标识通过人工检查。
10. 对所有 HTML 链接和 JavaScript 控制台错误进行检查，确保无死链、无未处理异常。
11. 不执行安装、提交、推送、部署、删除文件或修改 `.env`。

最后输出：修改文件清单、页面到接口映射、仍待后端实现的接口、mock 与真实模式切换方法，以及未解决风险。

## 八、患者端常驻 LLM 面板增量提示词

在上述规范基础上修改所有 `fronts/patient/*.html`，不要改变管理员端和医生端：

1. 桌面端在患者页面最右侧常驻宽 320–380px 的“页面助手”栏；中间内容自适应缩小。768px 以下改为右侧抽屉，360px 下由固定悬浮按钮打开，不能遮挡危机支持卡或主要提交按钮。
2. 同一组件在每次路由/页面切换后以当前 `page` 调用 `POST /api/v1/patient/page-assistant/respond`，`message` 为空，并展示 `page_intro`；同一页面刷新不连续重复朗读。
3. 输入问题时只提交当前页面枚举和文本，绝不提交患者 ID、出生日期或病历正文。发送期间禁用重复提交，回答区使用 `aria-live="polite"`。
4. `out_of_scope` 显示“前往对应页面”按钮，并只使用后端返回的 `suggested_page` 经本地白名单映射后跳转，禁止直接把返回值拼进 URL。
5. `care_navigation` 跳转或打开新增 `patient/care.html`。医生卡展示姓名、科室、职称、原接诊医生标记、状态、排队人数、本人位置、预计返岗时间和剩余休假天数；`unknown` 必须显示“状态待确认”。不得根据前端时钟猜测或改写医生状态。
6. `requires_guardian_support=true` 时显示温和且不羞辱的监护支持说明和后续入口；该提示不得覆盖或阻断危机求助信息。
7. `crisis_support` 在面板和页面顶部同时固定显示不可自动消失的支持卡，呈现后端 `crisis_contacts`，至少包含 12356、110、120；没有现实回执时禁止写“已通知医生/急救”。
8. 年龄组只控制表达与可访问性：`child` 用清楚、非命令式语言，`older_adult` 使用更大字号、清晰按钮和充足操作时间；不在前端显示出生日期或计算年龄。
9. 503 时仅将自由问答降级为“暂不可用”；页面说明可使用本地静态副本，医生状态与排队数据不可用 mock 冒充真实值。
10. 助手标题下持续展示“用于页面说明与就医导航，不替代医生诊断”。不得加入药物剂量、诊断结论、自动分诊或自动联系现实机构功能。

新增页面与接口映射：`patient/care.html` -> `/patient/care-navigation/doctors`；所有患者页的助手 -> `/patient/page-assistant/respond`。验收时覆盖键盘焦点、抽屉焦点锁定、Esc 关闭、断网、403、423、503、空医生列表、休假跨时区和重复点击。

## 九、跨页记忆、功能介绍和限次增量提示词

继续复用第八节的患者端常驻助手，不在浏览器中调用 DeepSeek：

1. 登录患者端后首次加载助手时令内存中的 `assistantSessionId=null`。首次 API 响应后保存 `data.session_id`；患者端页面间跳转时继续携带。若当前页面是多 HTML 页面，可存入 `sessionStorage`，键名仅为 `patient_assistant_session_id`；退出登录、401 或切换账号时清除。该 ID 不是认证凭证，所有请求仍使用 HttpOnly 会话 Cookie。
2. 每个患者页面加载完成后发送一次：

```json
{"page":"overview","feature_key":"page","event":"page_open","session_id":null,"message":""}
```

3. 每个功能按钮只在真正打开对应抽屉、对话框、步骤页或功能区域时发送一次 `feature_open`。不要在 hover、预加载、重复渲染或事件冒泡时计数。按钮到 `feature_key` 的映射必须严格使用接口文档白名单。
4. 普通提问发送 `event="message"` 和非空 `message`。翻页后调用 `GET /api/v1/patient/page-assistant/sessions/{session_id}/messages?limit=50` 恢复可见消息，再插入当前页面分隔文本；请求失败时显示可重试状态，不把本地缓存冒充服务端历史。
5. `page_intro` 在助手中作为轻量提示气泡展示，不伪装成用户消息。`usage_count` 仅用于调试或无障碍状态，不显示“监控次数”等可能引发不适的文案。
6. `introduction_suppressed=true` 或 `response_type="introduction_suppressed"` 时不要插入空气泡、不要弹 toast、不要再次请求介绍；用户主动提问仍正常发送。
7. 同一次页面加载用 `Set` 或 `data-assistant-introduced` 防止重复发送。请求失败时本次最多手动重试一次，禁止自动循环导致使用次数虚增。
8. 介绍内容必须直接展示后端模板响应，前端不得调用 DeepSeek、不得读取 `.env`、不得在 JavaScript、HTML、localStorage、sessionStorage 或网络日志保存 API Key。
9. 当前助手面板增加“记得本次浏览中的对话”说明，并提供“开始新对话”按钮。按钮只清除本地 `assistantSessionId`，下一次发送时由服务端新建会话；不得删除数据库审计日志。
10. 对话跨页后，在消息区插入无障碍分隔文本“已进入：{页面名称}”，但不要把该文本作为用户消息提交给后端。

验收：连续打开同一功能 8 次均显示介绍，第 9 次无空白气泡；换页后追问能引用上一页对话；切换患者账号不能复用会话；快速双击只计一次；401 清除 session ID；前端源码和网络请求中不存在 DeepSeek Key。

## 十、预约、医生超额弹窗与助理工作台增量提示词

在现有静态 Tailwind 前端上新增以下页面和交互；所有状态均来自 API，不在浏览器计算就诊次数、容量或队列：

1. 登录页增加“助理”身份，成功后按服务端角色进入 `assistant/index.html`；不能仅根据下拉框跳转。
2. `patient/care.html` 的医生卡增加未来两天日期、计划容量、当前预约人数、本人队列位置和“预约”按钮。资格不足时展示服务端返回的已完成次数和阈值，不自行统计。
3. 创建预约返回 `awaiting_patient_decision` 时打开模态框：“该医生预约已达到计划人数，是否更换医生？”按钮为“选择其他医生”和“仍请求该医生”。后者提交 `continue_request` 后显示“等待医生确认”，不能写“预约成功”。
4. `doctor/index.html` 增加未来两天容量设置卡，每个日期独立输入 0–1000；显示当前预约人数和版本。只允许医生修改自己的容量。
5. 医生端轮询或 SSE 获取 `/doctor/appointments/pending-decisions`。有新请求时显示可访问模态框，包含患者显示名、日期、队列位置和“接受/拒绝”。接受后明确提示“患者仍需排队”。
6. 医生拒绝时二次选择“直接告知患者”或“委婉告知患者”。提交期间保持弹窗，服务端成功后才关闭；不得让浏览器直接调用 DeepSeek。
7. 新增 `assistant/index.html`：三方协调队列、待医生处理超额预约、未来排班缺口和今日夜班医生。助理只能协调和排班，不能查看病历正文、诊断、量表答案或替医生接受预约。
8. 新增夜班排班组件：一个日期只显示一位医生；409 时刷新并提示“该日期已由其他人完成排班”，不能覆盖。
9. 助理导航：协调工作台、预约升级、夜班排班、个人中心。所有患者信息最小化展示，默认不显示证件、联系方式或对话正文。
10. 多账号验收使用不同浏览器 Profile 或无痕窗口同时登录至少：2 位患者、1 位医生、1 位助理。验证患者坚持超额预约后医生立即看到待处理项，医生决定后两位患者与助理看到一致状态；Cookie、sessionStorage 和助手会话不能串号。

必须覆盖：9/10 次资格边界、容量 0、容量恰好满、超额、重复预约、快速双击、医生拒绝两种通知、医生无响应、夜班 409、401/403/423/503、跨时区日期和移动端弹窗焦点管理。

## 十一、整合交付入口

- 全部前端修改意见：`SPECS/前端全部修改意见.md`
- 可直接粘贴给前端生成工具的助理端 Prompt：`SPECS/助理端生成Prompt.md`

后续前端实现以这两份整合文档为主；本文件前十节保留为需求来源和变更历史。若描述冲突，以安全边界更严格、接口状态更明确的一份为准。
