# Contributing

## 开发流程

1. 不使用真实患者数据、真实联系方式或可用 API Key。
2. 运行 `scripts/Setup-Dev.ps1` 建立 `.venv`。
3. 只修改任务相关文件，并补充相应测试。
4. 运行 `scripts/Run-Tests.ps1`。
5. PR 描述说明数据边界、权限变化、迁移和验证结果。

## 安全要求

- 不提交 `.env`、`private/`、数据库、凭据、token 或完整医疗正文。
- 医疗事实、预约状态、库存和风险等级不能由前端或 LLM 自行生成。
- LLM 工具默认只读；任何写操作必须有显式业务权限、状态机和审计。
- 不使用模型输出替代医生诊断、处方或危机处置。
